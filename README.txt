Es necesario tener sqlite:
sudo apt-get install sqlite3 libsqlite3-dev
Para ver la base datos se puede usar cualquiera de estas opciones
DB Browser for SQLite(herramienta gráfica):
    sudo apt install sqlitebrowser

sqlite3 CLI(herramienta de línea de comandos):
    sqlite3 mi_base_de_datos.db